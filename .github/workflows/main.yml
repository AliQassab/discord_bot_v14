name: Deploy 
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: SSH Remote Commands
        uses: appleboy/ssh-action@v1.0.3
        with:
          HOST: ${{ secrets.HOST }}
          # username: ${{ secrets.USER_NAME }}
          PASSWORD: ${{ secrets.PASSWORD }}
          # port: ${{ secrets.PORT }}
          
         
          script: whoami
             # cd discord_bot_v14
             # git pull
             # npm i
             # node stop src/app.js
             # node start src/app.js

      
